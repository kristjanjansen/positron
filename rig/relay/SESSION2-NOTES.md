
---
## Session 3 — 2026-08-25 13:19 EEST (agent resume)
- Launch state: battery 2% DISCHARGING (~20min est). POWER CRITICAL. Checkpointing after every step.
- Plan: Task A = one clean 4-phase handover-test.sh pass; Task B = loop-lag.sh perception-lag loop.
- 13:21 Pre-flight ✅: FaceTime HD Camera enumerates AND ffmpeg@7 avfoundation probe captured 3 frames (exit 0, no warnings). No leftover relay processes; ports 1935/8554/9997/8899 free. mediamtx at /opt/homebrew/bin/mediamtx; .last-input uid 5cfa5053…; /tmp/overlay.txt exists (empty).
- NOTE: final build's fallback tier with camera present = WEBCAM (not slate) — phases 0/2 of handover-test will show webcam frames with CLOCK burn-in. This is the intended final-build behavior (why camera availability gates Task A).
- 13:22 Starting stack: mediamtx -> gapless.sh (uplink+splicer). Battery still 2%/discharging.
- 21:58 RESUME (session 4, power restored): AC 'charging' 4%. No relay processes survived the battery kill (pgrep clean, ports 1935/8554/9997 free). uid 5cfa5053fffe478fffbb9b5c65773d34 confirmed via src/.last-input. Re-running camera pre-flight (8.5h since 13:21 pass, lid state unknown).
- 22:00 Pre-flight PASS again: FaceTime HD Camera enumerates [0], probe captured 3 frames exit 0 no warnings. Task A proceeds with webcam fallback tier.
- 22:00 Stack started: mediamtx pid 2280, gapless uplink 2293, splicer 2294. Splicer picked WEBCAM tier, RAI ungated, streaming to CF.
- 22:02 DEFECT #9 FOUND (upgrades the "cosmetic" warning): CLOCK drawtext rendered NOTHING — verified with local frame render (blank). ffmpeg error '%{localtime} requires at most 1 arguments': the \: escapes were consumed one level too early, localtime got %H,%M,%S as 3 args and the whole text expansion failed. ALSO the design's eif ms field was stream-t ms, not wall-clock ms (would have added a ±1s bimodal error to Task B).
- 22:03 Fix verified locally: r"text='%{localtime\:%T.%3N}'" renders 22:02:19.248 (t0 .170 + ~78ms ffmpeg startup). One clock, true ms. splicer.py edited (raw string also kills the SyntaxWarning). Restarting stack — this ends the current broadcast; new videoUID expected (Task A not yet started, so no loss).
- 22:05 CAMERA WEDGED (defect #10, environmental): worked 21:58-22:02 (probe passed, first stack run streamed webcam frames), then hung after that stack's pkill — the webcam leg ffmpeg matched NO pkill pattern, orphaned, died on SIGPIPE, and the AVFoundation session leaked daemon-side. avfoundation open now blocks indefinitely (probe hung 20s, killed). No sudo (password required) so cameracaptured/appleh13camerad cannot be restarted. Falling back to SLATE-tier Task A per dispatch instructions. Lesson: stack teardown must kill the LEG ffmpegs explicitly (they match none of splicer.py/gapless.sh/rtmps patterns).
- 22:06 Restarting stack with SPLICER_WEBCAM=0 (benched-webcam retries every 60s would punch 6s FIFO output gaps into the uplink during the 4-phase walk).
- 22:08 CF live: NEW videoUID 340414c32c21f02023d360a59a955ae0 (expected — prior broadcast ended at socket close). E2E clock verified in pixels off CF live edge: burned 22:07:44.137, grabbed t0 22:07:53.053 (~8.9s naive first-grab lag; includes startup + manifest trailing, not the Task B method yet). Slate renders TEHNILINE PAUS + clock. Proceeding to Task A 4-phase walk (slate fallback tier, SPLICER_WEBCAM=0).
- 22:12 Task A attempt 1 FAILED at phase 1 — DEFECT #11: live leg (RTSP pull) TTFB is 11.6s, the 6s warm-up grace kills it every time -> starve-loop LIVE respawns every 6s, zero bytes to CF for ~90s, CF ended the broadcast (socket open, grace expired; lifecycle live:false videoUID null). Encoder+mediamtx healthy throughout (path ready, tracks present).
- 22:14 Isolated: probe flags don't help (analyzeduration/probesize/nobuffer all ~11-12s). RTSP VIDEO-ONLY = 2.33s, RTMP A+V = 2.40s -> ffmpeg's RTSP demuxer waits for RTCP sender reports on BOTH streams before emitting synced A/V (~10s SR interval from mediamtx). FIX: spawn_live now pulls rtmp://localhost:1935/rig (same -c copy pipeline). Restarting stack for attempt 2; another new videoUID expected.
- 22:14 Attempt 2 stack up (slate, SPLICER_WEBCAM=0, RTMP live leg). CF live, videoUID f507c108c45933434edff446db3bfdd5. Battery charging. Running handover-test.sh attempt 2.
- 22:17 TASK A PASS ✅: handover-test attempt 2 — videoUID f507c108c45933434edff446db3bfdd5 SAME across phases 0/1/2/3, verdict "ONE broadcast across all phases". Frames verify real content switching: 0=slate+clock 22:13:26.069, 1=testsrc2 "LIVE 1787685260.377417", 2=slate+clock 22:14:49.749, 3=testsrc2 "LIVE 1787685340.187573". Fallback tier = SLATE (camera wedged, webcam tier disabled — dispatch-authorized fallback). Artifacts: handover-run-2026-08-25-2214.log, handover-frames-2214/, handover-feeder-2214.log in rig/relay/.
- 22:19 Task B: OCR leg validated standalone — ffprobe@7 lavfi movie+crop(430:80:0:0)+ocr(whitelist='0123456789\:.') reads e2e-clock.png as EXACTLY 22:07:44.137 (matches burn). Filter-level escaping trap: whitelist colon needs '...\:.' quoted form. rig/loop-lag.sh written per parked design. Short run (N=3) first.
- 22:20 loop-lag smoke N=3: 3/3 valid, lag 7.8-8.4s, grab_s 15-21s (ffmpeg multivariant probe dominates; probe-limit flags change nothing — lag stable 7.2-7.6s with them, so method robust). Full run N=20 sleep=1 starting; battery charging.
- 22:27 TASK B DONE ✅: loop-lag.sh N=20 sleep=1, 20/20 valid OCR reads. PIPELINE LAG p50 7.441s / p95 8.397s (min 5.983, max 8.448). Raw rows -> results/loop-lag.jsonl. Player-perceived = pipeline + player.latency(2.5-4.0s) => ~9.9-11.4s at p50. Clock offset at run end: +54.3ms +/- 17.6ms (sntp, cloudflare) — same-machine loop so it cancels; NOT corrected, noted only. Method notes: burned-ms clusters at .560-.568 = frames are first-frames of 2s segments (phase-locked quantization, validates OCR); grab_s 12-21s is ffmpeg multivariant probe overhead, does not affect lag (t0 anchored before grab); probe-limit flags change nothing.
- 22:28 SyntaxWarning "fix if trivial" — already fixed as part of the defect-#9 repair (CLOCK is now a raw string).
- 22:28 Teardown: stack + mediamtx killed, broadcast ended (lifecycle live:false), camera daemons still wedged (needs sudo killall VDCAssistant/appleh13camerad or reboot to restore webcam tier). Session-final videoUIDs minted this session: 340414c32c21f02023d360a59a955ae0 (pre-Task-A stack, ended by defect-#11 silence), f507c108c45933434edff446db3bfdd5 (Task A pass + Task B run). Battery 41% charging at close.
